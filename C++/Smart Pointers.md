Wraps around a pointer and ensures proper resource deallocation.
Defined in `<memory>`.

## See Also

**Resource Acquisition Is Initialization (RAII):** Binds resource management to the lifetime of an object i.e. resources are acquired during initialization, and released/cleaned-up during the destructor call of the object. 