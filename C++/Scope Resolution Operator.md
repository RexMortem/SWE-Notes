Allows you to access other scopes within your current scope.

For instance, it allows you to access global variables when they're overwritten by a local variable (the global is shadowed by the local)

Another example is that it allows you to define class methods from outside the class.